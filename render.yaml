[build]
  command = "composer install && npm install && npm run build"

[env]
  PATH = "/opt/render/.nix-profile/bin:/opt/render/.cargo/bin:/usr/local/bin:/usr/bin:/bin"
  PHP_VERSION = "8.2"
  COMPOSER_ALLOW_SUPERUSER = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
