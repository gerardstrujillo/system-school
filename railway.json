{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php artisan db:wipe --force && php artisan migrate --seed --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
